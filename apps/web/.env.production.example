# Production environment template for the frontend build.
# Cloudflare Pages / Vercel: set this in the project's Environment Variables
# dashboard (Production), not as a committed file.

# Your live backend URL (the Render service from render.yaml), no trailing slash.
VITE_API_URL=https://pixel-academy-api.onrender.com
